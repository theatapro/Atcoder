#include <bits/stdc++.h>
#define rep(i, l, r) for (int i = (l); i < (r); i++)
 
typedef long long ll;
const ll LINF = 1e18;
const int INF = 1e9;

using namespace std;


int	main(void)
{
	int a, b, c, d;
	cin >> a >> b >> c >> d;
	cout << (a+b)*(c-d) << endl;
	cout << "Takahashi" << endl;
}
#include <bits/stdc++.h>
#define rep(i, l, r) for (int i = (l); i < (r); i++)
 
typedef long long ll;
const ll LINF = 1e18;
const int INF = 1e9;

using namespace std;

ll dp[LINF];

int	main(void)
{
	ll N;
	cin >> N;
	rep(i, 0, N)
	{
		int a,b;
		cin >> a;
		b = a;
		while(b%2==0)
		{
			b
		}
	}

}